import { Component } from '@angular/core';

@Component({
  selector: 'app-hero',
  template: `
    <ng-container>
      <router-outlet />
    </ng-container>
  `,
  styles: ``,
})
export class HeroComponent {}
